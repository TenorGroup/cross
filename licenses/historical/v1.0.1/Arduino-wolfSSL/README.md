# v1.0.1 Arduino-wolfSSL 5.7.2 licence record

`COPYING` is the historical GPL version 2 text for the Arduino-wolfSSL 5.7.2
dependency used by firmware v1.0.1. It matches the file in application commit
`630149f` byte for byte. The published archive for that release is linked from
`THIRD_PARTY_NOTICES.md`.

Current dependency notices are retained under
`licenses/dependencies/Arduino-wolfSSL/` and `third_party/wolfssl/`.
